# oil-baron-web

Angular 20 client for Oil Baron. Presentation only — all rules live in the API / `OilBaron.Game`.

## Run

```powershell
# From repo root, start API first:
dotnet run --project src/OilBaron.Api

# Then:
npm install
npm start
```

Open `http://localhost:4200`.
